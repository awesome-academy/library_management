<%= review.name %>
<br>
<%= t"review.content" %><%= review.content %>
<br>
<%= time_ago_in_words(review.created_at) %>
<br>
<% if can? :destory, review %>
  <%= link_to t("review.destory"), [review.book, review],
    remote: true, method: :delete,
    data: { confirm: t("review.you_sure") } %>
<% end %>
<br>
<br>
